# Root requirements — used by the legacy modules at the repo root
# (data_ingestion, providers/, quantitative_models, language, theme,
# trade_thesis, ...). The FastAPI backend imports these via the
# backend.services._compat shim, so they need to stay installed in CI
# even after the Streamlit entry point retires.
#
# Streamlit + plotly were dropped on 2026-04-26 when app.py was retired.

pandas>=3.0.2
numpy>=2.4.4
scikit-learn>=1.8.0
yfinance>=0.2.30
requests>=2.33.1
openai>=1.50.0
python-dotenv>=1.0.0
lxml>=5.0.0
beautifulsoup4>=4.12.0
azure-monitor-opentelemetry>=1.5.0
azure-data-tables>=12.5.0
statsmodels>=0.14.6
arch>=8.0.0

# defusedxml — providers/news_rss.py parses untrusted RSS XML and
# uses defusedxml.ElementTree to satisfy bandit B314.
defusedxml>=0.7.1

# --- dev / test (installed by CI, also fine for local) ---
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-timeout>=2.4.0
