/**
 * Historical home for three.js WebGPU + TSL type shims. The contents
 * moved to `frontend/types/three-tsl.d.ts` once the `@types/three`
 * surface for `three/tsl` proved sufficient on its own. Kept as a
 * placeholder so existing imports / tooling references resolve.
 */
export {};
