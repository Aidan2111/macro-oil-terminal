/**
 * Historical home for three.js WebGPU type shims. The contents
 * moved to `frontend/types/webgpu-types.d.ts`. Kept as a
 * placeholder so existing imports / tooling references resolve.
 */
export {};
