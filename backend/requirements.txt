fastapi>=0.115.0
uvicorn[standard]>=0.46.0
pydantic>=2.9.0
python-dotenv>=1.0.0
sse-starlette>=2.1.0

# Phase 1 — real provider stack pulled in via lazy imports inside
# route handlers. Container warmup stays fast because main.py only
# imports FastAPI + stdlib at module level; pandas/yfinance/etc. only
# load when a route is hit.
pandas>=3.0.2
numpy>=2.4.4
requests>=2.31.0
yfinance>=1.3.0
statsmodels>=0.14.6
scikit-learn>=1.8.0
scipy>=1.10.0
arch>=8.0.0
lxml>=5.0.0
beautifulsoup4>=4.12.0
websockets>=12.0
openai>=2.0.0,<3.0.0
alpaca-py>=0.43.3

# Azure AI Foundry — Agent Service migration (FT-Foundry).
# Pulled at module level by ``backend.services.trade_thesis_foundry`` only
# when ``USE_FOUNDRY=true``; otherwise lazy-imported behind the flag so
# the existing AOAI path doesn't pay the import cost.
azure-ai-projects>=2.1.0
azure-identity>=1.25.3

# dev / test (CI installs these too)
pytest>=8.2.0
pytest-asyncio>=0.24.0
httpx>=0.27.0
