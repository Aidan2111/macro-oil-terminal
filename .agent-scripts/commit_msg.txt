feat(thesis): upgraded models + streaming + materiality + history + dual-mode UI

Azure infra:
* Two new deployments on oil-tracker-aoai:
  - gpt-4o (2024-11-20, GlobalStandard, cap 50) — Quick read mode
  - o4-mini (2025-04-16, GlobalStandard, cap 50) — Deep analysis mode
* App Settings AZURE_OPENAI_DEPLOYMENT_FAST=gpt-4o,
  AZURE_OPENAI_DEPLOYMENT_DEEP=o4-mini.

trade_thesis.py:
* generate_thesis(ctx, *, mode, stream_handler) — fast / deep / legacy modes.
* Streaming via stream=True; delta callback invoked per chunk; sync fallback
  on stream errors; malformed-JSON retry nudge preserved.
* Reasoning model path (o-family) auto-uses api_version 2025-04-01-preview,
  skips `temperature`, bumps max_completion_tokens to 4000.
* Schema gains required `reasoning_summary` field (short in fast mode,
  3–6 sentences in deep mode).
* Materiality: _materiality_fingerprint + context_changed_materially with
  thresholds Δ|Z|>0.3, Δpx>1.5%, slope flip, vol regime change, new EIA
  release detected.
* History + diff: read_recent_theses, diff_theses, history_stats —
  fed from data/trade_theses.jsonl.
* Thesis dataclass carries mode/latency_s/streamed/retried/one_line().

app.py Tab 4:
* Mode radio (Quick read / Deep analysis) routes to the right deployment.
* Streaming placeholder renders partial JSON chunks as they arrive.
* Regenerate button with per-session rate limit 30/hour; disabled state
  with copy explaining why.
* Last-refreshed + data-lag captions.
* Materiality warning callout when inputs shift since last run.
* Auto-refresh cadence sidebar (advanced-only): off / 5m / 30m / 1h.
  Cadence only burns tokens when material change is detected.
* "What changed" diff info-callout vs previous thesis (stance flip, ±conf,
  new/dropped risks, new catalysts).
* "How I'm thinking about this" reasoning-summary expander.
* "Recent theses" expander with last-10 table + stats caption.
* Run-meta caption: mode · latency · streamed · retried · guardrails.

Live verification:
* gpt-4o streaming: 7.43s, stance=short_spread (conf 7.5).
* o4-mini deep streaming: 28.55s, stance=short_spread (conf 7.0),
  richer reasoning_summary.
* First o4-mini attempt failed with "api versions 2024-12-01-preview
  and later" — fixed by per-call version auto-upgrade.
