fix(quant): pandas 3.x DataFrame.fillna(method=) removed → use .ffill()

CI on Python 3.12 (pandas 3.x-track) fails with
  TypeError: NDFrame.fillna() got an unexpected keyword argument 'method'
on quant.regime_breakdown.

Replace the deprecated form with the direct .ffill() method.

Also:
* app.py: subheader "Brent vs WTI — price and spread dislocation"
  (Z-score suffix only when advanced view is on) — stops the last
  stats-jargon leftover in Tab 1.
* .agent-scripts/capture_screens.py updated tab locators to the new
  plain-language tab names so future screenshot runs don't time out.
* docs/screenshots refreshed against the new labels.
