feat: email alert stub (SMTP) wired to sidebar toggle

- alerts.py: maybe_send_zscore_alert with four return states
  (None below threshold / [would-send] preview when SMTP env unset /
  [sent] on success / [error] on failure). Env-var driven, no keys in repo.
- Sidebar toggle "Email me on Z-score breach"; Tab 1 renders preview
  (unconfigured) / success (sent) / warning (error) surfaces.
- test_runner: 3 new alert checks (below threshold, preview, negative-Z
  breach). Runner now 31/31 green.
