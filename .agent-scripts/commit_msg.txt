feat(quant): pass-2 review — vol-regime guardrail, liveness, keep-warm tune

Following the second desk-quant review (`docs/quant_review_2026-04-22_pass2.md`):

* Vol-regime guardrail (new 3rd clamp in `_apply_guardrails`):
  when `vol_spread_1y_percentile > 85` the suggested position size
  is capped at 2% of capital with a caveat. Already-modest sizes
  are left alone. Two unit tests (clamp fires; below-cap untouched).

* Keep-warm cadence tuned: 10-min during 07:00-22:00 UTC (from 5-min),
  30-min overnight. Saves GitHub Actions minutes on the free tier.

* Liveness annotation above the Trade Thesis card — rotates between
  dislocation-persistence, hours-since-last-EIA, and half-life
  summary. Tiny but makes the card feel live on every rerun.

* docs/quant_review_2026-04-22_pass2.md — K.N. second look.
  What improved, next top-5 ranked (CFTC positioning, Kalman hedge
  ratio, carry-aware backtest, trade blotter promoted, weekly
  petroleum CSV). Sizing verdict bumped pre-pass-2 → $5m sleeve.

124 unit tests green, smoke test clean.
