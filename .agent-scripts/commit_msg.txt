perf+ci: cache heavy compute, preconnect hints, scheduled keep-warm

- app.py: @st.cache_data wrappers around compute_spread_zscore,
  forecast_depletion, and backtest — keyed by (frame fingerprint,
  params). Slider changes no longer re-run heavy math.
- app.py: preconnect/dns-prefetch hints to jsdelivr.net and
  threejs.org so Three.js + Earth textures start fetching earlier.
- .github/workflows/keep-warm.yml: cron */5 07-22 UTC hitting
  /_stcore/health. F1's 20s cold-start should stay hidden from users
  during waking hours.

Baseline recorded in docs/perf/baseline.json:
  Cold   TTFB 1.44s  TTI 2.25s  T-chart 5.29s  transfer 3.2MB
  Warm   TTFB 0.83s  TTI 12.15s  T-chart 14.61s  transfer 3.3MB

Next: measure after, log delta in PROGRESS.md.
