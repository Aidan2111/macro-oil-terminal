test: pytest unit suite (71 tests, 73.5% cov) + Playwright e2e (13 tests)

Unit suite (tests/unit/, 71 tests @ 73.52% branch coverage):
* conftest with autouse env-scrub, EIA fixture monkeypatch, synth prices,
  sample ThesisContext + backtest fixtures.
* Full coverage of data_ingestion (real EIA via fixture + placeholder
  notice + InventoryUnavailable propagation), quantitative_models
  (spread, depletion, backtest incl. slippage/commission monotonicity,
  walk-forward, Monte Carlo percentiles, regime breakdown),
  trade_thesis (schema, deployment resolver, guardrails,
  fingerprint, materiality, diffs, streaming handler via mocked
  AzureOpenAI, stream-fallback-to-sync, malformed-JSON retry),
  thesis_context helpers + end-to-end build_context, provider impls
  (_yfinance happy+sad, _fred keyed, _aisstream helpers + require-key),
  observability no-op safety, webgpu template placeholders, input
  clamp, workflow files, alerts.

E2E suite (tests/e2e/, 13 Playwright tests — 84s green locally):
* Session-scope headless Streamlit on free port with /_stcore/health wait.
* Dashboard smoke: title, all 4 plain-language tabs, ticker strip,
  Dislocation-dominates-Z-score, AI tab body, "What would make us wrong",
  URL query-param roundtrip.
* Thesis flow: mode toggle shows gpt-4o + o4-mini, Regenerate button,
  Recent theses expander, Things-to-keep-in-mind, disclaimer, context
  expander exposes snapshot fields.

CI split (.github/workflows):
* ci.yml now runs pytest (3.11 + 3.12 matrix with coverage.xml artifact)
  + streamlit-smoke + legacy test_runner.py in parallel.
* e2e.yml new job: Playwright Chromium + pytest tests/e2e.

pyproject.toml: pytest + coverage config (70% fail_under).
