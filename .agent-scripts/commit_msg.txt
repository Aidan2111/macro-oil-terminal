feat+ops: App Insights, Azure alerts, Bicep IaC, backtest realism

Ops / observability
* Application Insights component oil-tracker-ai (westus2) created,
  connection string wired into App Service settings (never in repo).
* observability.py: defensive azure-monitor-opentelemetry wrapper —
  no-op when env var missing, never raises.
* Metric alerts wired (action group → Aidan's email):
  - oil-tracker-http5xx (Http5xx total > 5 in 5m, sev 2)
  - oil-tracker-slow-response (HttpResponseTime avg > 5s, sev 3).
* infra/main.bicep + infra/deploy.sh capture the whole stack (RG,
  plan, Web App + settings + streamlit startup, AOAI + gpt-4o-mini,
  AI component, action group, both alerts) reproducibly from scratch.
  Idempotent; `what-if` preview supported.

Backtest realism
* backtest_zscore_meanreversion: slippage + commission now applied per
  completed trade.
* walk_forward_backtest: 12m/3m rolling-window stats.
* monte_carlo_entry_noise: 200 runs with ±0.15σ threshold noise —
  surfaces threshold overfit.
* regime_breakdown: low-vol vs high-vol trade performance, median-split
  on 30d realised spread vol.
* Tab 1 expander renders the walk-forward bar chart, MC percentile
  metrics, and regime bar. Sidebar has slippage + commission inputs.

Tests: 36/36 green. Smoke test green. CD ready.
