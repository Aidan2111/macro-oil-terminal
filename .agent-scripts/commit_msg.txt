feat+ops: live ticker, URL state, Dependabot/CodeQL/pre-commit, release automation, docs

User-facing:
* Real-time ticker strip — 1-min yfinance intraday inside
  st.fragment(run_every=60) when the new "Live mode" sidebar toggle is
  on. Handles weekend/holiday/closed-feed by falling back to the daily
  series with a clear "market closed" badge and last-bar timestamp.
* URL state sync — sidebar z/floor/window round-trip into
  st.query_params so links capture the current view. Values are clamped
  (NaN/inf/non-numeric → safe default) via a new _clamp helper.

Ops + security:
* .github/dependabot.yml — weekly pip + github-actions + docker PRs.
* .github/workflows/codeql.yml — Python security+quality queries
  on every push, PR, and Monday 04:00 UTC.
* .pre-commit-config.yaml — opt-in gitleaks + ruff + basic hygiene hooks.
* .github/CODEOWNERS — @Aidan2111 owns everything (explicit on
  infra-sensitive paths).
* SECURITY.md — reporting + secret-handling posture + documented
  CSP/header limitation (Streamlit default).

Release automation:
* .github/workflows/release.yml — triggers on v*.*.* tag (+ manual
  dispatch). Test-gate → OIDC login → zip deploy → health check → GitHub
  Release with release notes generated from commits + PROGRESS.md diff
  since the previous tag. Signed with the same SP used for CD.

Docs:
* docs/architecture.md — Mermaid data-flow + deployment diagrams,
  trade-off notes.
* CONTRIBUTING.md — quick start, test gate, CD expectations, data policy.

Tests:
* test_runner.py +8 checks (clamp valid/NaN/non-numeric/±inf, plus
  workflow-file sanity: CD OIDC + test gate, release notes builder,
  Dependabot present). 32/32 green.
