name: keep-warm

on:
  schedule:
    # Every 10 minutes during waking hours (07:00-22:00 UTC)
    - cron: "*/10 7-22 * * *"
    # Every 30 minutes overnight (23:00-06:00 UTC) — saves free-tier CPU
    - cron: "*/30 23,0-6 * * *"
  workflow_dispatch:

concurrency:
  group: keep-warm
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Ping /_stcore/health
        run: |
          set -eu
          URL="https://oil-tracker-app-4281.azurewebsites.net/_stcore/health"
          for attempt in 1 2 3; do
            if curl -fsS --max-time 20 "$URL"; then
              echo "  warm on attempt $attempt"
              exit 0
            fi
            echo "  attempt $attempt failed, retrying..."
            sleep 10
          done
          echo "Health endpoint cold after 3 attempts" >&2
          exit 1
